{
  "name": "shieldpass",
  "private": true,
  "version": "0.0.0",
  "engines": { "pnpm": ">=9" },
  "scripts": {
    "build": "pnpm -r build",
    "test:contracts": "cd packages/contracts && forge test -vv"
  }
}
